#!/usr/bin/env rune

Copyright 2007 Hewlett Packard, under the terms of the MIT X license
found at http://www.opensource.org/licenses/mit-license.html ..................

# rune(["~/e/src/esrc/scripts/test/updoc/char-arith.updoc"])

    ? pragma.syntax("0.9")

    ? 'b' - 'a'
    # value: 1

    ? 'b' - 1
    # value: 'a'

    ? 'b' + 1
    # value: 'c'

    ? 'b' + 'a'
    # problem: <ClassCastException: Character doesn't coerce to an int32>
